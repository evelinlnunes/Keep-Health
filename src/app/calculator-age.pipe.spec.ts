import { CalculatorAgePipe } from './calculator-age.pipe';

describe('CalculatorAgePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculatorAgePipe();
    expect(pipe).toBeTruthy();
  });
});
